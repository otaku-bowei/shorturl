# 短链服务 - 验证方案

## 项目信息

- **项目路径**: `/Volumes/zhitai2/git/Java/shorturl`
- **技术栈**: Spring Boot 3.2 + JPA + Redis + MySQL
- **端口**: 8080

---

## 一、验证步骤

### 1.1 启动服务

```bash
cd /Volumes/zhitai2/git/Java/shorturl

# 编译项目
./mvnw clean package -DskipTests

# 启动服务
./mvnw spring-boot:run
```

**注意**: 需要先启动 Redis 和 MySQL

### 1.2 启动 Redis

```bash
# Mac
brew services start redis

# 或 Docker
docker run -d -p 6379:6379 redis:alpine
```

### 1.3 启动 MySQL

```bash
# Mac
brew services start mysql

# 或 Docker
docker run -d -p 3306:3306 -e MYSQL_ROOT_PASSWORD= -e MYSQL_DATABASE=shorturl mysql:8
```

---

## 二、功能验证

### 2.1 创建短链

```bash
# 请求
curl -X POST http://localhost:8080/api/shorturl \
  -H "Content-Type: application/json" \
  -d '{
    "originalUrl": "https://example.com/very/long/url/path",
    "expireDays": 30
  }'

# 响应
{
  "id": 1,
  "shortKey": "19i1",
  "shortUrl": "http://localhost:8080/19i1",
  "originalUrl": "https://example.com/very/long/url/path",
  "expireTime": "2025-06-18T00:00:00",
  "clickCount": 0,
  "createdAt": "2025-05-18T21:00:00"
}
```

### 2.2 跳转

```bash
# 请求
curl -v http://localhost:8080/api/shorturl/redirect/19i1

# 响应 (302)
# Location: https://example.com/very/long/url/path
```

### 2.3 获取信息

```bash
# 请求
curl http://localhost:8080/api/shorturl/19i1
```

---

## 三、测试验证

### 3.1 单元测试

```bash
# 运行单元测试
./mvnw test -Dtest=ShortUrlServiceTest
```

**预期结果**:
- ✅ 创建短链成功
- ✅ 从缓存获取成功
- ✅ 从数据库获取成功
- ✅ 短链不存在抛异常
- ✅ 短链过期抛异常

### 3.2 集成测试

```bash
# 运行集成测试
./mvnw test -Dtest=ShortUrlControllerTest
```

**预期结果**:
- ✅ 创建短链 API
- ✅ 参数校验
- ✅ 跳转 API

### 3.3 高并发压测

```bash
# 运行压测
./mvnw test -Dtest=ConcurrencyTest
```

**测试场景**:

| 测试项 | 并发数 | 请求数 | 预期 QPS |
|--------|--------|--------|----------|
| 短链生成 | 100 | 10000 | > 1000 |
| 跳转测试 | 100 | 10000 | > 2000 |
| 唯一性 | 200 | 10000 | 无冲突 |
| 10000 TPS | 500 | 30000 | > 5000 |

---

## 四、性能指标

### 4.1 预期性能

| 指标 | 目标值 |
|------|--------|
| 短链生成 QPS | > 1000 |
| 跳转 QPS | > 2000 |
| 平均延迟 | < 50ms |
| 缓存命中率 | > 90% |

### 4.2 优化建议

1. **Redis 集群**: 生产环境使用 Redis 集群
2. **预生成 ID**: 预先生成 ID 放入队列
3. **批量写入**: 批量 INSERT 到 MySQL
4. **CDN**: 跳转使用 302 + CDN

---

## 五、验证检查清单

- [ ] Redis 启动成功
- [ ] MySQL 启动成功
- [ ] 服务启动成功 (8080)
- [ ] 创建短链成功
- [ ] 跳转成功 (302)
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 高并发测试通过

---

## 六、常见问题

### Q1: Redis 连接失败

```bash
# 检查 Redis
redis-cli ping
# 应返回 PONG
```

### Q2: 端口被占用

```bash
# 查找占用端口的进程
lsof -i :8080

# 杀掉进程
kill -9 <PID>
```

### Q3: 测试失败

```bash
# 查看详细日志
./mvnw test -Dtest=ConcurrencyTest -Dspring.profiles.active=test
```
